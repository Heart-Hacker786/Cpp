#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int count1,count2,count;
    cin.read((char*)&count1,sizeof(int));
    int list1[count1];
    cin.read((char*)list1,sizeof(int)*count1);
    int count1;
    cin.read((char*)&count2,sizeof(int));
    int list2[count2];
    cin.read((char*)list2,sizeof(int)*count1);
    
    for(i=0;i<count1;i++){
        for(j=0j<count2;j++){
            if(list1[i]==list2[j]){
                count++;
                for(c=0;c<count;c++){
                    if(arr[c]==list[i];){
                        break;
                    }
                    
                }
            }
        }
    }
    return 0;
}
